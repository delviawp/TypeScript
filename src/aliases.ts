type Combinable = number | string

//with type aliases we can just assign the value 
// and it's more reusable instead of writing number | string all over again

function combine (
    input1: number | string,
    input2: number 
)